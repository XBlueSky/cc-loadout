---
import Layout from '../layouts/Layout.astro';
import { loadSiteData } from '../lib/manifest.mjs';

const { plugin } = await loadSiteData();
---
<Layout title="cc-loadout — One plugin. Every loadout." description={plugin.tagline}>
  <h1>One plugin. Every loadout.</h1>
  <p>{plugin.intro}</p>
</Layout>
